

if (window.File && window.FileReader && window.FileList && window.Blob) { // Great success! All the File APIs are supported. 
			alert("open dir")
   ad = window.FileReader('/')
    console.log(as)
}
else {
    alert('The File APIs are not fully supported in this browser.'); 
}